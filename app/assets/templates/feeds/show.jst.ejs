<!-- FEED SHOW -->
<h3><%= feed.escape("title") %></h3>

<ul>
<% feed.get('entries').each(function(entry) { %>
  <li>
    <a href="#/feeds/<%= feed.id %>/entries/<%= entry.id %>">
      <%= entry.escape("title")%>
    </a>
  </li>
<% })%>
</ul>